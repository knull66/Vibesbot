services:
  - type: web
    name: vibesbot
    runtime: python
    buildCommand: pip install -r requirements-web.txt
    startCommand: uvicorn src.web_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
